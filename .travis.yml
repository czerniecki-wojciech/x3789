sudo: false

language: cpp

compiler: gcc

addons:
  apt:
    packages:
      - cmake
      - g++-4.8
      - libxrandr-dev
      - libx11-dev
      - libxxf86vm-dev
      - libxi-dev

before_script:
    - mkdir build
    - cd build
    - cmake ..

script: make

notifications:
    email:
        - czerniecki.wojciech@gmail.com
    on_success: change
    on_failure: always

